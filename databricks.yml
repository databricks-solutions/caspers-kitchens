# databricks.yml
bundle:
  name: caspers-kitchens

variables:
  catalog:
    description: "UC catalog for this bundle/target"
    default: caspersdev

workspace:
  # Default root path under your user home; still configurable per target
  root_path: /Workspace/Users/${workspace.current_user.userName}/caspers-kitchens-demo
  file_path: ${workspace.root_path}     # where files get synced

# Ship the repo files on deploy (adjust excludes as you like)
sync:
  include:
    - ./*
  exclude:
    - .git/**
    - .DS_Store
    - .claude/**
    - .databricks/**
    - .venv/**
    - .bundle/**
    - ./data/universe/**
    - __pycache__/**
    - "*.pyc"
    - images/**
    - docs/**
    - apps/caspersai-terminal/node_modules/**
    - apps/ceo-dashboard/app.yaml
    - apps/caspersai-terminal/logs/**
    - apps/caspersai-terminal/test/**
    - apps/supportconsolek/**/.databricks/**
    - apps/supportconsolek/**/node_modules/**
    - apps/supportconsolek/**/dist/**
    - apps/ceo-dashboard/**/__pycache__/**
    - apps/ceo-dashboard/**/*.pyc

scripts:
  cleanup:
    content: |
      set -eo pipefail

      # Detect the target passed to `databricks bundle run cleanup -t <target>`
      TARGET_FLAG=""
      if env | grep -q '^DATABRICKS_BUNDLE_TARGET='; then
        TARGET_FLAG="-t $DATABRICKS_BUNDLE_TARGET"
      fi

      # Get fully-resolved bundle config (includes target overrides)
      RESOLVED_JSON="$(databricks bundle validate $TARGET_FLAG --output json)"

      # Pull values we need (requires jq)
      FILE_PATH="$(printf '%s' "$RESOLVED_JSON" | jq -r '.workspace.file_path // (.workspace.root_path + "/src")')"

      CATALOG="$(printf '%s' "$RESOLVED_JSON" | jq -r '.variables.catalog.value // .variables.catalog.default')"
      ENV_CATALOG="$(printenv BUNDLE_VAR_catalog 2>/dev/null || true)"
      if [ -n "$ENV_CATALOG" ]; then
        CATALOG="$ENV_CATALOG"
      fi

      echo "Cleaning up catalog: $CATALOG"

      mkdir -p .bundle
      cat > .bundle/submit.json <<JSON
      {
        "run_name": "caspers: cleanup (ephemeral)",
        "tasks": [
          {
            "task_key": "destroy",
            "notebook_task": {
              "notebook_path": "$FILE_PATH/destroy",
              "source": "WORKSPACE",
              "base_parameters": { "CATALOG": "$CATALOG" }
            }
          }
        ]
      }
      JSON

      databricks jobs submit --json @.bundle/submit.json


targets:
  default:
    default: true

    resources:
      jobs:
        caspers:
          name: "Casper's Initializer"
          queue:
            enabled: true
          performance_target: PERFORMANCE_OPTIMIZED

          parameters:
            - name: CATALOG
              default: ${var.catalog}
            - name: EVENTS_VOLUME
              default: events
            - name: LLM_MODEL
              default: databricks-meta-llama-3-3-70b-instruct
            - name: REFUND_AGENT_ENDPOINT_NAME
              default: ${var.catalog}_refund_agent
            - name: SIMULATOR_SCHEMA
              default: simulator
            - name: START_DAY
              default: "70"
            - name: SPEED_MULTIPLIER
              default: "60.0"
            - name: SCHEDULE_MINUTES
              default: "3"
            - name: PIPELINE_SCHEDULE_MINUTES
              default: "0"

          tasks:
            - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/canonical_data

            - task_key: Spark_Declarative_Pipeline
              depends_on:
                - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/lakeflow

            - task_key: Refund_Recommender_Agent
              depends_on:
                - task_key: Spark_Declarative_Pipeline
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/refunder_agent

            - task_key: Refund_Recommender_Stream
              depends_on:
                - task_key: Spark_Declarative_Pipeline
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/refunder_stream

            - task_key: Lakebase_Reverse_ETL
              depends_on:
                - task_key: Refund_Recommender_Stream
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/lakebase

            - task_key: Databricks_App_Refund_Manager
              depends_on:
                - task_key: Lakebase_Reverse_ETL
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/apps

  complaints:
    resources:
      jobs:
        caspers:
          name: "Casper's Initializer"
          queue:
            enabled: true
          performance_target: PERFORMANCE_OPTIMIZED

          parameters:
            - name: CATALOG
              default: ${var.catalog}
            - name: EVENTS_VOLUME
              default: events
            - name: LLM_MODEL
              default: databricks-meta-llama-3-3-70b-instruct
            - name: COMPLAINT_AGENT_ENDPOINT_NAME
              default: ${var.catalog}_complaint_agent
            - name: COMPLAINT_RATE
              default: "0.15"
            - name: SIMULATOR_SCHEMA
              default: simulator
            - name: START_DAY
              default: "70"
            - name: SPEED_MULTIPLIER
              default: "60.0"
            - name: SCHEDULE_MINUTES
              default: "3"
            - name: PIPELINE_SCHEDULE_MINUTES
              default: "0"

          tasks:
            - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/canonical_data

            - task_key: Spark_Declarative_Pipeline
              depends_on:
                - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/lakeflow

            - task_key: Complaint_Agent
              depends_on:
                - task_key: Spark_Declarative_Pipeline
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/complaint_agent

            - task_key: Complaint_Generator_Stream
              depends_on:
                - task_key: Spark_Declarative_Pipeline
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/complaint_generator_stream

            - task_key: Complaint_Agent_Stream
              depends_on:
                - task_key: Complaint_Agent
                - task_key: Complaint_Generator_Stream
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/complaint_agent_stream

            - task_key: Complaint_Lakebase
              depends_on:
                - task_key: Complaint_Agent_Stream
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/complaint_lakebase

  free:
    resources:
      jobs:
        caspers:
          name: "Casper's Initializer"
          queue:
            enabled: true
          performance_target: PERFORMANCE_OPTIMIZED

          parameters:
            - name: CATALOG
              default: ${var.catalog}
            - name: EVENTS_VOLUME
              default: events
            - name: SIMULATOR_SCHEMA
              default: simulator
            - name: START_DAY
              default: "70"
            - name: SPEED_MULTIPLIER
              default: "60.0"
            - name: SCHEDULE_MINUTES
              default: "3"
            - name: PIPELINE_SCHEDULE_MINUTES
              default: "3"

          tasks:
            - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/canonical_data

            - task_key: Spark_Declarative_Pipeline
              depends_on:
                - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/lakeflow

  menus:
    resources:
      jobs:
        caspers:
          name: "Casper's Initializer"
          queue:
            enabled: true
          performance_target: PERFORMANCE_OPTIMIZED

          parameters:
            - name: CATALOG
              default: ${var.catalog}
            - name: EVENTS_VOLUME
              default: events
            - name: LLM_MODEL
              default: databricks-meta-llama-3-3-70b-instruct
            - name: REFUND_AGENT_ENDPOINT_NAME
              default: ${var.catalog}_refund_agent
            - name: MENU_KNOWLEDGE_ENDPOINT_NAME
              default: ${var.catalog}_menu_knowledge
            - name: INSPECTION_KNOWLEDGE_ENDPOINT_NAME
              default: ${var.catalog}_inspection_knowledge
            - name: MENU_SUPERVISOR_ENDPOINT_NAME
              default: ${var.catalog}_menu_supervisor
            - name: SIMULATOR_SCHEMA
              default: simulator
            - name: START_DAY
              default: "70"
            - name: SPEED_MULTIPLIER
              default: "60.0"
            - name: SCHEDULE_MINUTES
              default: "3"
            - name: PIPELINE_SCHEDULE_MINUTES
              default: "0"

          tasks:
            - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/canonical_data

            - task_key: Menu_Data
              max_retries: 0
              depends_on:
                - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/menu_data

            - task_key: Inspection_Data
              max_retries: 0
              depends_on:
                - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/inspection_data

            - task_key: Menu_Pipeline
              max_retries: 0
              depends_on:
                - task_key: Menu_Data
                - task_key: Inspection_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/menu_pipeline

            - task_key: Menu_Genie
              max_retries: 0
              depends_on:
                - task_key: Menu_Pipeline
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/menu_genie

            - task_key: Menu_Knowledge_Agent
              max_retries: 0
              depends_on:
                - task_key: Menu_Pipeline
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/menu_knowledge_agent

            - task_key: Inspection_Knowledge_Agent
              max_retries: 0
              depends_on:
                - task_key: Inspection_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/inspection_knowledge_agent

            - task_key: Menu_Supervisor
              max_retries: 0
              depends_on:
                - task_key: Menu_Genie
                - task_key: Menu_Knowledge_Agent
                - task_key: Inspection_Knowledge_Agent
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/menu_supervisor

  support:
    resources:
      jobs:
        caspers:
          name: "Casper's Initializer"
          queue:
            enabled: true
          performance_target: PERFORMANCE_OPTIMIZED

          parameters:
            - name: CATALOG
              default: ${var.catalog}
            - name: EVENTS_VOLUME
              default: events
            - name: LLM_MODEL
              default: databricks-meta-llama-3-3-70b-instruct
            - name: SUPPORT_AGENT_ENDPOINT_NAME
              default: ${var.catalog}_support_agent
            - name: SUPPORT_RATE
              default: "0.18"
            - name: SIMULATOR_SCHEMA
              default: simulator
            - name: START_DAY
              default: "70"
            - name: SPEED_MULTIPLIER
              default: "60.0"
            - name: SCHEDULE_MINUTES
              default: "3"
            - name: PIPELINE_SCHEDULE_MINUTES
              default: "0"

          tasks:
            - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/canonical_data

            - task_key: Spark_Declarative_Pipeline
              depends_on:
                - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/lakeflow

            - task_key: Support_Request_Generator_Stream
              depends_on:
                - task_key: Spark_Declarative_Pipeline
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/support_request_generator_stream

            - task_key: Support_Request_Agent
              depends_on:
                - task_key: Support_Request_Generator_Stream
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/support_request_agent

            - task_key: Support_Request_Agent_Stream
              depends_on:
                - task_key: Support_Request_Generator_Stream
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/support_request_agent_stream

            - task_key: Support_Lakebase_And_App
              depends_on:
                - task_key: Support_Request_Agent_Stream
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/support_lakebase

  all:
    resources:
      jobs:
        caspers:
          name: "Casper's Initializer"
          queue:
            enabled: true
          performance_target: PERFORMANCE_OPTIMIZED

          parameters:
            - name: CATALOG
              default: ${var.catalog}
            - name: EVENTS_VOLUME
              default: events
            - name: LLM_MODEL
              default: databricks-meta-llama-3-3-70b-instruct
            - name: REFUND_AGENT_ENDPOINT_NAME
              default: ${var.catalog}_refund_agent
            - name: COMPLAINT_AGENT_ENDPOINT_NAME
              default: ${var.catalog}_complaint_agent
            - name: COMPLAINT_RATE
              default: "0.15"
            - name: MENU_KNOWLEDGE_ENDPOINT_NAME
              default: ${var.catalog}_menu_knowledge
            - name: INSPECTION_KNOWLEDGE_ENDPOINT_NAME
              default: ${var.catalog}_inspection_knowledge
            - name: MENU_SUPERVISOR_ENDPOINT_NAME
              default: ${var.catalog}_menu_supervisor
            - name: SIMULATOR_SCHEMA
              default: simulator
            - name: START_DAY
              default: "70"
            - name: SPEED_MULTIPLIER
              default: "60.0"
            - name: SCHEDULE_MINUTES
              default: "3"
            - name: PIPELINE_SCHEDULE_MINUTES
              default: "0"

          tasks:
            - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/canonical_data

            - task_key: Spark_Declarative_Pipeline
              depends_on:
                - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/lakeflow

            - task_key: Refund_Recommender_Agent
              depends_on:
                - task_key: Spark_Declarative_Pipeline
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/refunder_agent

            - task_key: Refund_Recommender_Stream
              depends_on:
                - task_key: Spark_Declarative_Pipeline
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/refunder_stream

            - task_key: Complaint_Agent
              depends_on:
                - task_key: Spark_Declarative_Pipeline
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/complaint_agent

            - task_key: Complaint_Generator_Stream
              depends_on:
                - task_key: Spark_Declarative_Pipeline
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/complaint_generator_stream

            - task_key: Complaint_Agent_Stream
              depends_on:
                - task_key: Complaint_Agent
                - task_key: Complaint_Generator_Stream
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/complaint_agent_stream

            - task_key: Complaint_Lakebase
              depends_on:
                - task_key: Complaint_Agent_Stream
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/complaint_lakebase

            - task_key: Lakebase_Reverse_ETL
              depends_on:
                - task_key: Refund_Recommender_Stream
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/lakebase

            - task_key: Databricks_App_Refund_Manager
              depends_on:
                - task_key: Lakebase_Reverse_ETL
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/apps

            - task_key: Menu_Data
              depends_on:
                - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/menu_data

            - task_key: Inspection_Data
              depends_on:
                - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/inspection_data

            - task_key: Menu_Pipeline
              depends_on:
                - task_key: Menu_Data
                - task_key: Inspection_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/menu_pipeline

            - task_key: Menu_Genie
              depends_on:
                - task_key: Menu_Pipeline
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/menu_genie

            - task_key: Menu_Knowledge_Agent
              depends_on:
                - task_key: Menu_Pipeline
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/menu_knowledge_agent

            - task_key: Inspection_Knowledge_Agent
              depends_on:
                - task_key: Inspection_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/inspection_knowledge_agent

            - task_key: Menu_Supervisor
              depends_on:
                - task_key: Menu_Genie
                - task_key: Menu_Knowledge_Agent
                - task_key: Inspection_Knowledge_Agent
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/menu_supervisor

  ceo:
    variables:
      catalog:
        default: devconnect_demo
    resources:
      jobs:
        caspers:
          name: "Casper's Initializer"
          queue:
            enabled: true
          performance_target: PERFORMANCE_OPTIMIZED

          parameters:
            - name: CATALOG
              default: ${var.catalog}
            - name: EVENTS_VOLUME
              default: events
            - name: LLM_MODEL
              default: databricks-meta-llama-3-3-70b-instruct
            - name: SIMULATOR_SCHEMA
              default: simulator
            - name: START_DAY
              default: "70"
            - name: SPEED_MULTIPLIER
              default: "60.0"
            - name: SCHEDULE_MINUTES
              default: "3"
            - name: PIPELINE_SCHEDULE_MINUTES
              default: "0"
            # CEO-specific KA endpoints
            - name: INSPECTION_KNOWLEDGE_ENDPOINT_NAME
              default: ${var.catalog}_inspection_knowledge
            - name: CEO_LEGAL_KA_ENDPOINT
              default: ${var.catalog}_ceo_legal_knowledge
            - name: CEO_REGULATORY_KA_ENDPOINT
              default: ${var.catalog}_ceo_regulatory_knowledge
            - name: CEO_AUDIT_KA_ENDPOINT
              default: ${var.catalog}_ceo_audit_knowledge
            - name: CEO_CONSULTANCY_KA_ENDPOINT
              default: ${var.catalog}_ceo_consultancy_knowledge
            - name: CEO_SUPERVISOR_ENDPOINT_NAME
              default: ${var.catalog}_ceo_supervisor

          tasks:
            - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/canonical_data

            - task_key: Spark_Declarative_Pipeline
              depends_on:
                - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/lakeflow

            - task_key: Inspection_Data
              max_retries: 0
              depends_on:
                - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/inspection_data

            - task_key: CEO_Document_Data
              max_retries: 0
              depends_on:
                - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/ceo_document_data

            - task_key: CEO_Revenue_Genie
              max_retries: 0
              depends_on:
                - task_key: Spark_Declarative_Pipeline
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/ceo_genie_spaces

            - task_key: Inspection_Knowledge_Agent
              max_retries: 0
              depends_on:
                - task_key: Inspection_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/inspection_knowledge_agent

            - task_key: CEO_Knowledge_Agents
              max_retries: 0
              depends_on:
                - task_key: CEO_Document_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/ceo_knowledge_agents

            - task_key: CEO_Supervisor
              max_retries: 0
              depends_on:
                - task_key: CEO_Revenue_Genie
                - task_key: Inspection_Knowledge_Agent
                - task_key: CEO_Knowledge_Agents
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/ceo_supervisor

            - task_key: CEO_Lakebase
              max_retries: 0
              depends_on:
                - task_key: Canonical_Data
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/ceo_lakebase

            - task_key: CEO_App
              max_retries: 0
              depends_on:
                - task_key: CEO_Supervisor
                - task_key: CEO_Lakebase
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/ceo_app

            - task_key: CEO_Readiness_Check
              max_retries: 0
              depends_on:
                - task_key: CEO_App
              notebook_task:
                notebook_path: ${workspace.root_path}/stages/ceo_readiness_check

            - task_key: CEO_Evaluation
              max_retries: 0
              depends_on:
                - task_key: CEO_Readiness_Check
              notebook_task:
                notebook_path: ${workspace.root_path}/demos/ceo-demo/ceo_evaluation
