{
  "properties": {
    "demo_name": {
      "type": "string",
      "default": "my_demo",
      "description": "Name for your demo (used for bundle and job naming)",
      "order": 1
    },
    "caspers_catalog": {
      "type": "string",
      "default": "caspersdev",
      "description": "Casper's UC catalog (must match your main deployment)",
      "order": 2
    },
    "root_path_name": {
      "type": "string",
      "default": "caspers-demo-{{.demo_name}}",
      "description": "Directory name for workspace deployment (Consider setting to the same directory as the main Casper's demo deployment)",
      "order": 3
    }
  },
  "success_message": "\nYour demo bundle '{{.demo_name}}' has been created!\n\nNext steps:\n1. Add notebooks to notebooks/\n2. Update databricks.yml with your demo logic\n3. Run: databricks bundle deploy\n4. Run: databricks bundle run {{.demo_name}}_demo"
}
